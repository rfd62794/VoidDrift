# Copy to .deploy.env (gitignored) and fill in. Used by deploy_telemetry.ps1.
SSH_HOST=
SSH_USER=
SSH_PORT=22
# Optional: private key authorized on the server. Leave empty to use ssh-agent
# or the default ~/.ssh/id_* keys.
SSH_KEY=
