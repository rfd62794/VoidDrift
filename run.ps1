# VoidDrift — Desktop Development Runner
# Reads assets/balance.toml at runtime — edit TOML and re-run without rebuilding Rust.
# Note: Dynamic linking disabled on Windows due to linker object limit (LNK1189).

cargo run
